================================================================================
                        ASSET REGISTER - CONTINUITY DOCUMENT
================================================================================
Last Updated: 2026-01-31 (Refactoring Phase)
Maintainer: [Your Name Here]

================================================================================
                              PROJECT OVERVIEW
================================================================================

This is an Asset Register application built on Cloudflare Workers with a React
frontend. It provides a web interface for managing and tracking assets through
a RESTful API.

Tech Stack:
- Backend: Cloudflare Workers (TypeScript)
- Frontend: React with React Router 7.9.6
- Build Tool: Vite
- Database: SQLite (via Cloudflare D1)
- Configuration: wrangler.json, tsconfig files

================================================================================
                           PROJECT STRUCTURE
================================================================================

/                           # Root directory
├── .gitignore              # Git ignore rules
├── package.json            # Node dependencies and scripts
├── vite.config.ts          # Vite configuration
├── wrangler.json           # Cloudflare Workers configuration
├── tsconfig.json           # TypeScript config (root)
├── tsconfig.node.json      # TypeScript config (Node tools)
├── tsconfig.cloudflare.json# TypeScript config (Cloudflare)
├── worker-configuration.d.ts # Worker type definitions
├── README.md               # Project documentation
├── CONTINUITY.TXT          # This file
├── migrations/             # Database migrations
│   ├── 0001_initial_schema.sql
│   ├── 0002_add_asset_fields.sql
│   └── 0003_add_depreciation_fields.sql
├── app/                    # Application source
│   ├── app.css             # Global styles
│   ├── root.tsx            # App root component
│   ├── entry.server.tsx    # Server entry point
│   ├── routes.ts           # Route definitions
│   ├── components/         # React components (modular UI pieces)
│   ├── constants/          # [NEW] Shared constants
│   │   ├── index.ts        # Barrel export
│   │   ├── assetTypes.ts   # IRD_ASSET_TYPES, ASSET_CATEGORIES, MOCK_ADDRESSES
│   │   └── colors.ts       # COLORS array for room highlighting
│   ├── hooks/              # [NEW] Custom React hooks
│   │   ├── index.ts        # Barrel export
│   │   ├── useAssetWizard.ts    # Asset creation/editing wizard state
│   │   ├── useOwnershipWizard.ts # Ownership questions wizard state
│   │   ├── useImportWizard.ts   # CSV import wizard state
│   │   └── useDrawingCanvas.ts  # Drawing tool state
│   ├── routes/             # Route handlers
│   │   ├── home.tsx        # Home page route (2700+ lines, needs refactoring)
│   │   ├── transactions.tsx # FY Working/Register views (696 lines)
│   │   └── api.registers.ts # API route for registers
│   ├── services/           # API and business logic services
│   │   ├── api.ts          # Main API service
│   │   ├── depreciationService.ts # Depreciation calculations
│   │   └── registerApi.ts  # Register-specific API
│   ├── types/              # TypeScript type definitions
│   │   ├── index.ts        # Main types (Asset, Room, Register, etc.)
│   │   └── assetTypes.ts   # Asset-specific types
│   ├── utils/              # [NEW] Shared utility functions
│   │   ├── index.ts        # Barrel export
│   │   ├── currency.ts     # formatCurrency(), currencyCellStyle
│   │   ├── dates.ts        # calculateFinancialPeriod(), formatDate(), parseDate()
│   │   └── csv.ts          # parseCSV(), parseCSVLine(), generateCSV()
│   └── welcome/            # Welcome page component
│       └── welcome.tsx
└── workers/
    └── app.ts              # Worker entry point

================================================================================
                           DATABASE SCHEMA
================================================================================

The database schema is defined in migrations/0001_initial_schema.sql.
Please refer to that file for the complete schema documentation.

Key tables (if applicable):
- registers: Main table for asset registers
- assets: Individual asset entries
- [Add other tables as needed]

To apply migrations:
  npx wrangler d1 migrations apply <database_name>

================================================================================
                         DEVELOPMENT COMMANDS
================================================================================

Install dependencies:
  npm install

Start development server:
  npm run dev

Build for production:
  npm run build

Deploy to Cloudflare:
  npx wrangler deploy

Run type checking:
  npm run check

Preview deployment:
  npm run preview

================================================================================
                       CLOUDFLARE CONFIGURATION
================================================================================

The application uses Cloudflare Workers and D1 database. Configuration is in
wrangler.json. Key settings include:

- Main worker: workers/app.ts
- Database: D1 database for persistence
- Assets: Static asset serving

To set up a new environment:
1. Update wrangler.json with your Cloudflare account details
2. Create D1 database: npx wrangler d1 create asset-register
3. Update wrangler.json with the database ID
4. Apply migrations: npx wrangler d1 migrations apply asset-register
5. Deploy: npx wrangler deploy

================================================================================
                          API DOCUMENTATION
================================================================================

The API routes are defined in app/routes/api.registers.ts. Key endpoints:

GET  /api/registers      - List all registers
POST /api/registers      - Create a new register
GET  /api/registers/:id  - Get specific register
PUT  /api/registers/:id  - Update register
DELETE /api/registers/:id - Delete register

Check app/routes/api.registers.ts for complete API documentation.

================================================================================
                          IMPORTANT NOTES
================================================================================

1. TypeScript Configuration:
   - Cloudflare-specific types are in tsconfig.cloudflare.json
   - Standard TypeScript rules are in tsconfig.json

2. React Router:
   - Routes are defined in app/routes.ts
   - Uses file-based routing pattern

3. Static Assets:
   - Images and static files go in public/
   - Build output is served from workers/

4. Environment Variables:
   - Check wrangler.json for required environment variables
   - Use .dev.vars for local development secrets

5. Database Migrations:
   - All migrations must be placed in migrations/
   - Filename format: 0001_description.sql
   - Migrations are applied in filename order

================================================================================
                           KNOWN ISSUES / TODOS
================================================================================

REFACTORING STATUS (In Progress):
---------------------------------
The codebase was identified as having oversized files. A refactoring effort
is underway to improve maintainability:

[x] Step 1: Extract shared utility functions (~/utils/)
[x] Step 2: Extract constants to /app/constants/
[x] Step 3: Fix transactions.tsx imports (now 696 lines, down from 790)
[x] Step 4: Update home.tsx to use shared constants (now 2726 lines, down from 2866)
[x] Step 5: Create custom hooks (~/hooks/) - hooks created, not yet integrated
[ ] Step 6: Integrate hooks into home.tsx - NEXT STEP
[ ] Step 7: Create view components to further reduce home.tsx

TARGET: Reduce home.tsx from 2700+ lines to ~500-700 lines

HOME.TSX REMAINING WORK:
- Replace ~15 asset wizard useState hooks with useAssetWizard()
- Replace ~8 ownership wizard useState hooks with useOwnershipWizard()
- Replace ~3 import wizard useState hooks with useImportWizard()
- Replace ~6 drawing canvas useState hooks with useDrawingCanvas()
- Extract renderShape() and related functions to SitePlanRenderer component
- Extract depreciation calculation to use depreciationService
- Extract CSV parsing to use ~/utils/csv.ts

================================================================================
                            EMERGENCY CONTACTS
================================================================================

Cloudflare Support: https://developers.cloudflare.com/support/
Documentation: https://developers.cloudflare.com/workers/

================================================================================
